require "spec_helper"

RSpec.describe Spotify::Pause do
  it "has a version number" do
    expect(Spotify::Pause::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
